import { createTheme } from "@mui/material";
const theme = createTheme({
  palette: {
    primary: { main: '#fff' },
    buttonColor: { main: '#1e88e5', contrastText: '#fff' },
    grayButtonColor: { main: '#757575' }
  }
})

export default theme;